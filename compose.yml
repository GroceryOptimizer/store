services:
  store1:
    container_name: ${STORE1_NAME}
    build: .
    image: store:latest
    environment:
      - STORE_NAME=Ica
    volumes:
      - ./docker/store1.json:/app/products.json
  store2:
    container_name: store_coop
    image: store:latest
    environment:
      - STORE_NAME=Coop
    volumes:
      - ./docker/store1.json:/app/products.json
  store3:
    container_name: store_willys
    image: store:latest
    environment:
      - STORE_NAME=Willys
    volumes:
      - ./docker/store1.json:/app/products.json
